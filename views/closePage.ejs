<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Redirection</title>
    <style>
        body {
            text-align: center;
            font-family: 'Cairo', Arial, sans-serif;
        }

        .error {
            color: red;
        }

        #logContainer {
            margin-top: 20px;
            border: 1px solid #ccc;
            padding: 10px;
            max-height: 200px;
            overflow-y: auto;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <!-- Include the Messenger Extensions SDK -->
    <script>
        window.extAsyncInit = function () {
            // the Messenger Extensions JS SDK is done loading
            logMessage('Messenger Extensions JS SDK is done loading.');
        };

        function sendMessageAndClose() {
            // Use MessengerExtensions.getUserID to obtain the PSID
            MessengerExtensions.getUserID(
                function success(uids) {
                    var psid = uids.psid;
                    logMessage('User ID obtained: ' + psid);

                    // Send a message to the user
                    MessengerExtensions.sendMessageToUser(psid, {
                        text: 'Hello, this is a message from your website!'
                    }, function success() {
                        // The message was sent successfully
                        logMessage('Message sent successfully');

                        // Close the web browser
                        MessengerExtensions.requestCloseBrowser(function success() {
                            // Browser closed successfully
                            logMessage('Web browser closed');
                        }, function error(err) {
                            // Error closing the browser
                            logMessage('Error closing web browser: ' + err);
                        });
                    }, function error(err) {
                        // Error sending the message
                        logMessage('Error sending message: ' + err);
                    });
                },
                function error(err, errorMessage) {
                    // Error handling code
                    logMessage('Error obtaining user ID: ' + errorMessage);
                }
            );
        }

        function logMessage(message) {
            var logContainer = document.getElementById('logContainer');
            var logEntry = document.createElement('div');
            logEntry.textContent = message;
            logContainer.appendChild(logEntry);

            // Log to the console
            console.log(message);

            // Scroll to the bottom of the log container
            logContainer.scrollTop = logContainer.scrollHeight;
        }
    </script>
    <!-- Include the Messenger Extensions SDK -->
    <script>
        (function (d, s, id) {
            var js, fjs = d.getElementsByTagName(s)[0];
            if (d.getElementById(id)) { return; }
            js = d.createElement(s); js.id = id;
            js.src = "https://connect.facebook.com/en_US/messenger.Extensions.js";
            fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'Messenger'));
    </script>
</head>
<body>
    <div class="error"></div>
    <div id="logContainer"></div>

    <!-- Add this button inside the body of your HTML document -->
    <button onclick="sendMessageAndClose()">Send Message and Close</button>
</body>
</html>
